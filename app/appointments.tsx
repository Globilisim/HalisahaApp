import React from 'react';
import Dashboard from './index';

// Appointments page serves the same grid layout as the main index for now,
// or can be a more specialized view.
export default function AppointmentsPage() {
    return <Dashboard />;
}
